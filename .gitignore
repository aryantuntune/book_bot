__pycache__/
*.pyc
*.pyo
.pytest_cache/
.venv/
venv/

# Runtime data — never commit (contains real customer phone numbers)
Input/
Output/
Issues/
recordings/
logs/
*.xlsx
*.xlsx.tmp

# Local tooling state
.claude/

# Persistent chromium profile (cookies, local storage, service-worker cache)
# — private to each operator, never commit. Wildcards cover suffixed profiles
# created by --profile-suffix (.chromium-profile-2/, .chromium-profile-alt/ …).
.chrome-profile/
.chrome-profile-*/
.chromium-profile/
.chromium-profile-*/

# PyInstaller build artifacts
build/
dist/
*.spec
!booking_bot.spec
